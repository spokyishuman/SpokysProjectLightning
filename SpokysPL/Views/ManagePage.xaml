<UserControl x:Class="SpokysProjectVercel.Views.ManagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1200"
      Background="Transparent">

    <Grid Margin="40,36,40,40" Focusable="True"
          KeyDown="Grid_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="SearchBox"
                         Text="Search installed games..."
                         GotFocus="SearchBox_GotFocus"
                         TextChanged="SearchBox_TextChanged"
                         Style="{DynamicResource {x:Type TextBox}}"/>
                <Button Grid.Column="1" Content="🔄 Refresh" Style="{DynamicResource AccentButton}"
                        Margin="12,0,0,0" Click="Refresh_Click"/>
                <TextBlock Grid.Column="2" x:Name="GameCount" FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <ItemsControl x:Name="GamesList" Margin="0,0,0,20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{DynamicResource ModernCard}" Padding="14" Margin="0,0,0,10"
                                    BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1"
                                    Effect="{DynamicResource CardShadow}">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="▶ Play" Tag="{Binding}" Click="ContextPlay_Click"/>
                                        <MenuItem Header="🌐 Open Steam Page" Tag="{Binding AppId}" Click="ContextOpenSteam_Click"/>
                                        <MenuItem Header="📂 Open Install Folder" Tag="{Binding InstallDir}" Click="ContextOpenFolder_Click"/>
                                        <MenuItem Header="📋 Copy App ID" Tag="{Binding AppId}" Click="ContextCopyId_Click"/>
                                        <Separator/>
                                        <MenuItem Header="🗑 Remove" Tag="{Binding}" Click="ContextRemove_Click"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="60" Height="60" CornerRadius="10"
                                        Background="{DynamicResource SurfaceBrush}" Margin="0,0,12,0"
                                        ClipToBounds="True">
                                    <Grid>
                                        <Border Background="#141F19">
                                            <TextBlock Text="🎮" FontSize="20"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Image Source="{Binding CoverUrl}" Stretch="UniformToFill"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Grid>
                                </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding AppId, StringFormat='App {0}'}" FontSize="11"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBlock Text=" · " FontSize="11"
                                                       Foreground="{DynamicResource MutedForegroundBrush}"/>
                                            <TextBlock Text="{Binding SizeDisplay}" FontSize="11"
                                                       Foreground="{DynamicResource MutedForegroundBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="▶ Play" Style="{DynamicResource AccentButton}"
                                            Tag="{Binding}" Click="Play_Click"
                                            VerticalAlignment="Center" Padding="16,8" FontSize="12" Margin="0,0,8,0"/>
                                    <Button Grid.Column="3" Content="🗑" Style="{DynamicResource SecondaryButton}"
                                            Tag="{Binding}" Click="Remove_Click"
                                            VerticalAlignment="Center" Width="34" Height="34" Padding="0" FontSize="13"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty state -->
                <Border x:Name="EmptyState" Visibility="Collapsed"
                        Background="{DynamicResource CardBrush}" CornerRadius="16"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="32" Margin="0,40,0,0"
                        HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📦" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="No games installed yet" FontSize="20" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Go to the Add page to install game manifests" FontSize="13"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBlock Text="Or drop fix archives on the Home page" FontSize="12"
                                   Foreground="{DynamicResource MutedForegroundBrush}"
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" x:Name="PaginationPanel"
                    Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0"/>
    </Grid>
</UserControl>


